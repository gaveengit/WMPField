<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ucsc.mit.wmp">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="WMP Field"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WMP">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".LoginActivityController"
            android:label="WMP Field">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OvListActivity"></activity>
        <activity android:name=".MainMenuActivity"></activity>
        <activity android:name=".MapsActivity2"></activity>
        <activity android:name=".MapsActivity"></activity>
        <activity android:name=".AddBgMainActivity"></activity>
        <activity android:name=".AddBgAdditionalActivity"></activity>
        <activity android:name=".AddOvMainActivity"></activity>
        <activity android:name=".AddOvAdditionalActivity"></activity>
        <activity android:name=".AddMrcMainActivity"></activity>
        <activity android:name=".AddMrcAdditionalActivity"></activity>
        <activity android:name=".IncidentContactActivity"></activity>
        <activity android:name=".IncidentDescriptionActivity"></activity>
        <activity android:name=".IncidentLocationActivity"></activity>
        <activity android:name=".AddBgServiceMainActivity"></activity>
        <activity android:name=".AddBgServiceAdditionalActivity"></activity>
        <activity android:name=".AddBgCollectionMainActivity"></activity>
        <activity android:name=".AddBgCollectionAdditionalActivity"></activity>
        <activity android:name=".AddOviServiceMainActivity"></activity>
        <activity android:name=".AddOviServiceAdditionalActivity"></activity>
        <activity android:name=".AddOviCollectionMainActivity"></activity>
        <activity android:name=".AddOviCollectionAdditionalActivity"></activity>
        <activity android:name=".AddMrcServiceMainActivity"></activity>
        <activity android:name=".AddMrcServiceAdditionalActivity"></activity>
        <activity android:name=".AddMrcReleaseMainActivity"></activity>
        <activity android:name=".AddMrcReleaseAdditionalActivity"></activity>
        <activity android:name=".OviDataSyncActivity"></activity>
    </application>

</manifest>